echo SKIP_PREFLIGHT_CHECK=true >> .env && echo off && if not exist "config" mkdir "config" && if not exist "routes" mkdir "routes" && if not exist "models" mkdir "models" && cd client && echo SKIP_PREFLIGHT_CHECK=true >> .env && npm i && cd src && if not exist "views" mkdir "views" && if not exist "components" mkdir "components" && if not exist "utils" mkdir "utils" && cd ../../ && call npm install