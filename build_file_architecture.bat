echo SKIP_PREFLIGHT_CHECK=true >> .env && echo off && npm i && if not exist "config" mkdir "config" && if not exist "routes" mkdir "routes" && if not exist "models" mkdir "models" && cd client && npm i && cd src && if not exist "views" mkdir "views" && if not exist "components" mkdir "components" && if not exist "utils" mkdir "utils"  